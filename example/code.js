const declaration = {
    aa: 'third',
    A: 'second',
    bb: 'fifth',
    0: 'first',
    B: 'fourth',
};

const spreadable = {
    filler: 'obj',
};

const toast = {
    top: 'slice',
    ...spreadable,
    bottom: 'slice',
};

(function({ the, object, destructor, pattern }) {})();
